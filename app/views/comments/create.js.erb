setTimeout(function() {
  // $('#new_comment').html("");
  $("#all-comments").html("<%= j(render partial: 'comments/comments', locals: {commentable: @commentable}) %>");
  $("#all_comments").fadeIn();
  $('#comment-input').val("");
}, 200);

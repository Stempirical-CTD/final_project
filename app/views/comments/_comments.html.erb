<div id="comment-box">
<% commentable.comments.each do |comment| %>
  <div class="comment-user"><%= User.find_by_id(comment.user_id).username %></div>
  <%= comment.body %><br>
  <br>
<% end %>
</div>
